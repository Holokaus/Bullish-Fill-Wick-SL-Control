# OPERATION FILLPOINT — AMENDMENT 1 (owner directive, 2026-08-24/25)

Owner review verdict on W2/W3: the kill stands for the NAIVE concept, but the study
did not yet mine the conditional structure. New directives:

1. Deep statistical atlas REQUIRED: per asset×timeframe INDEPENDENTLY (assets have
   individual personalities), covering:
   - optimal wick LENGTH (dose-response over uw_frac deciles, not one tercile cut)
   - trend-following context (trailing return, SMA alignment)
   - the candle BEFORE the event candle (direction, body share, expansion, streaks)
   - event-candle VOLUME (relative to its own recent average)
   - candle TYPE (body share, lower wick share, marubozu/doji character)
   - PATH to target: P(hit within 2 candles), time-to-hit distribution,
     bounces/crossings before the hit, adverse excursion before the hit
   - targets BEYOND the wick (1.5x wick gap) — "we are not targeting cents"
2. Thresholds are QUALITY GATES to prevent unprofitable trades (selectivity > frequency).
3. Conversion bar unchanged: any candidate system must pass locked TRAIN gates
   (pooled CI>0, N>=800, >=2 assets positive unless the rule is explicitly per-asset,
   non-knife-edge neighbors, worst regime > -15 bps), then ONE E-VAL shot on
   Jan2025-Jun2026, then lockbox. Per-asset rules allowed but must be frozen per-asset
   BEFORE E-VAL and judged per-asset (family = the set of frozen rules).

4. INFRASTRUCTURE NOTE: F: drive unmounted mid-operation on 2026-08-25; all V2 artifacts
   were lost with it. Study rebuilt at C:\Users\A\Bullish-Fill-Wick\V2\ from in-context
   sources + surviving raw klines on C:. Lockbox re-downloaded EXTENDED through Aug 25.
   Determinism preserved via fixed seeds; results expected identical up to seed effects.

STATUS: W4-atlas declared EXPLORATION, TRAIN-only. Nothing here confirms anything;
it generates hypotheses with multiplicity control (BH within each feature family).
